# Module 3 Task Checklist

- [x] Update `backend/server.js` schema to support `firestoreId` and `assignedVolunteerName`.
- [x] Update `ReportEmergencyPage.vue` and `ReportEmergency.vue` to send `firestoreId` to backend API.
- [x] Update `components/Dashboard.vue` to fetch volunteer details, compute eligibility/matching/distance, rank matches, and add the "My Assigned Emergencies" section.
- [x] Update `components/EmergencyCard.vue` to show matching status, distance, and the "View Details" button.
- [x] Update `pages/EmergencyDetailPage.vue` to implement transaction-based acceptance (`runTransaction`) and dual status sync to MongoDB.
- [x] Update `pages/MyEmergenciesPage.vue` to display the assigned volunteer's name.
- [x] Verify everything works (eligibility, matching, ranking, acceptance transactions, sync).
